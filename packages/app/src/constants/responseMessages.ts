import { IErrorMessage } from "../types/response/error.types"
import { ISuccessMessage } from "../types/response/success.types"

// Any for now as its just a setup
export const OK_ResponseMessage: Record<any, string> = {
}

export const NotOK_ResponseMessage: Record<any, string> = {
}